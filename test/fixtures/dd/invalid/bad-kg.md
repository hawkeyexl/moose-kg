---
title: Bad KG
kg:
  altLabels: [orphaned]
  bogus: true
---

# Bad KG

Two schema failures in one block: `altLabels` without the `prefLabel` it describes,
and a key the `kg` block does not define.
