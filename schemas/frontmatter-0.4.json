{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://moose-tools.dev/kg/schemas/frontmatter-0.4.json",
  "title": "moose-kg knowledge-graph frontmatter (0.4)",
  "description": "SKOS concept fields plus PROV-O provenance under the top-level `kg` key, consumed by moose-kg build and fill. 0.4 changes kg.provenance to an array with one entry per model, so multi-model fills keep truthful attribution; the 0.2/0.3 single-object form remains accepted. Files without a `kg` key pass.",
  "type": "object",
  "properties": {
    "kg": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "prefLabel": {
          "type": "string",
          "minLength": 1,
          "description": "Preferred label of the concept this document is primarily about."
        },
        "altLabels": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "uniqueItems": true,
          "description": "Alternative labels (synonyms, abbreviations) for the primary concept."
        },
        "broader": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "uniqueItems": true,
          "description": "Labels of broader (parent) concepts."
        },
        "narrower": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "uniqueItems": true,
          "description": "Labels of narrower (child) concepts."
        },
        "related": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "uniqueItems": true,
          "description": "Labels of associatively related concepts."
        },
        "subjects": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "uniqueItems": true,
          "description": "Subject labels for this document; behaves like tags."
        },
        "revisionOf": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "uniqueItems": true,
          "description": "Repo-relative paths or URLs of earlier documents this one revises (prov:wasRevisionOf)."
        },
        "derivedFrom": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "uniqueItems": true,
          "description": "Repo-relative paths or URLs this document was derived from (prov:wasDerivedFrom)."
        },
        "generatedBy": {
          "type": "string",
          "minLength": 1,
          "description": "Model or tool that generated this document's content (prov:wasGeneratedBy)."
        },
        "provenance": {
          "oneOf": [
            {
              "type": "array",
              "items": { "$ref": "#/$defs/provenanceEntry" },
              "description": "One attribution entry per model (written by moose-kg fill)."
            },
            { "$ref": "#/$defs/provenanceEntry" }
          ],
          "description": "Attribution for machine-proposed kg fields; humans delete entries they have reviewed. The single-object form is the deprecated 0.2/0.3 shape."
        }
      },
      "dependentRequired": {
        "altLabels": ["prefLabel"],
        "broader": ["prefLabel"],
        "narrower": ["prefLabel"],
        "related": ["prefLabel"]
      }
    }
  },
  "$defs": {
    "provenanceEntry": {
      "type": "object",
      "additionalProperties": false,
      "required": ["generatedBy"],
      "properties": {
        "generatedBy": {
          "type": "string",
          "minLength": 1,
          "description": "Model that proposed the machine-filled kg fields."
        },
        "fields": {
          "type": "array",
          "items": {
            "enum": ["prefLabel", "altLabels", "broader", "narrower", "related", "subjects"]
          },
          "uniqueItems": true,
          "description": "Which kg fields this model proposed (written by moose-kg fill)."
        }
      }
    }
  }
}
