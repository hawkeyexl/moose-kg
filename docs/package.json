{
  "name": "moose-kg-docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The moose-kg documentation site. A separate npm project so the published package never carries a docs toolchain.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.1",
    "astro": "^7.0.2"
  }
}
