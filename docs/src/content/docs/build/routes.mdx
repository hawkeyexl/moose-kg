---
title: Map your site's routes
description: If your docs link by published URL rather than file path, dockg cannot resolve those links until you tell it how routes map back to source files. The broken links that remain afterwards are the ones worth fixing.
---

import { Aside, CardGrid, LinkCard, Steps, Tabs, TabItem } from '@astrojs/starlight/components';

You ran `dockg stats` and got a long list of broken internal links that are not broken. They
work fine on your published site. Your corpus links by route, and dockg resolved those links
against the filesystem.

By the end of this page those links will be real graph edges. The broken-link list will then
contain only links that are actually broken.

## Before you start

You need a `graph.ttl` and a broken-link list from [`dockg stats`](/dockg/build/). Keep the
current count, because you will compare against it.

## Why this is not automatic

A page writes `[find](/docs/actions/find)`. Your site generator publishes that correctly because
it knows `/docs/actions/find` is served from `src/content/docs/actions/find.mdx`. That mapping
lives in the generator's config, in a convention, or in both.

dockg does not read your generator's config. It would need one adapter per generator, and they
change. It takes the mapping from you instead, once.

## Configure the mapping

A route entry answers two questions. What URL prefix does the site serve, and which repo
directory serves it?

<Tabs>
  <TabItem label="Astro / Starlight">
    ```yaml title="dockg.config.yaml"
    routes:
      - basePath: /docs
        root: src/content/docs
        extensions: [".md", ".mdx"]
    ```
  </TabItem>
  <TabItem label="Docusaurus">
    ```yaml title="dockg.config.yaml"
    routes:
      - basePath: /docs
        root: docs
        extensions: [".md", ".mdx"]
    ```
  </TabItem>
  <TabItem label="MkDocs">
    ```yaml title="dockg.config.yaml"
    routes:
      - basePath: /
        root: docs
        extensions: [".md"]
    ```
  </TabItem>
</Tabs>

| Key | Default | What it does |
|---|---|---|
| `root` | **required** | The repo directory the route serves. |
| `basePath` | `/` | The URL prefix. Normalized to a leading `/` with no trailing `/`; `/` means site root. |
| `extensions` | `[".md", ".mdx"]` | Tried in order when the URL has no extension. |
| `indexFiles` | `["index", "README"]` | Resolves a directory URL like `/docs/actions/`. |

So `/docs/actions/find` becomes `src/content/docs/actions/find.mdx`, and `/docs/actions/` becomes
`src/content/docs/actions/index.mdx`.

## Multiple routes are normal

A docs section and a blog usually publish under different prefixes from different directories.
List them all; each is matched independently.

```yaml title="dockg.config.yaml"
routes:
  - basePath: /docs
    root: src/content/docs
  - basePath: /blog
    root: src/content/blog
```

<Aside type="note" title="Route mappings are config-only">
  There is no CLI flag. Routes define what the corpus *means*, so a graph built with different
  route rules is a different graph. It should not vary with how someone typed the command.
</Aside>

## Verify by delta

<Steps>

1. **Rebuild and re-run stats.**

   ```sh
   dockg build && dockg stats
   ```

   The broken-link count should drop sharply. If it does not move at all, your `root` probably
   does not match where the files actually live.

2. **Inspect one edge rather than trusting the number.**

   ```sh
   dockg query -p dcterms:references
   ```

   Confirm a link you know is route-style now points at the source file you expect.

</Steps>

## The remaining broken links are the point

This is the outcome worth caring about. Before mapping, the broken-link list was noise you had to
ignore. After mapping, everything left in it is a link that genuinely resolves to nothing. That
is usually a page moved or deleted without its inbound links being updated.

That list is now small enough to act on, and stable enough to
[gate on in CI](/dockg/govern/ci/).

Links to files that are not documents are not in it. Your `extensions` list says what a document
looks like in this corpus. A target carrying some other explicit extension is read as a static
asset your site serves, not as a document dockg failed to find. That covers a downloadable
archive, a linked PDF, an OpenAPI `.yaml`, and dockg's own
[`/dockg/ns.ttl`](/dockg/ns.ttl). There is no Markdown file you could add to fix such a finding,
so dockg does not raise one.

The check that matters is unchanged. `/docs/actions/missing` and `/docs/actions/missing/` are
still broken, and so is `/docs/actions/missing.mdx`. You wrote a document target and it does not
resolve. Only a non-document extension is skipped.

<Aside type="note" title="Route mapping applies to component links too">
  A route written as `<LinkCard href="/docs/actions/find" />` in an `.mdx` file resolves through the
  same mapping as a Markdown link. If your site is component-heavy, expect the broken-link count to
  move substantially the first time you configure routes.
</Aside>

## What you have now

Site-style links now resolve to real `dcterms:references` edges. Impact analysis reflects how
your documentation actually cross-references itself, and the broken-link count means something.

## Where to go next

<CardGrid>
  <LinkCard
    title="Govern it in CI"
    href="/dockg/govern/ci/"
    description="Make the broken-link count a gate, so the next moved page fails its pull request."
  />
  <LinkCard
    title="Configuration reference"
    href="/dockg/reference/configuration/"
    description="The full routes[] key reference, including normalization rules."
  />
</CardGrid>
