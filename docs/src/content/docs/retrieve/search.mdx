---
title: Build the search artifacts
description: Export a lexical index beside the graph, compute vectors locally with no API call, and understand the staleness rules that make dockg refuse a mismatched sidecar rather than rank with it.
---

import { Aside, CardGrid, LinkCard, Steps } from '@astrojs/starlight/components';

Retrieval needs two artifacts beside the graph: a lexical index, and optionally a vector sidecar.
This page builds both and explains what keeps them honest.

## Before you start

A built `kg/graph.ttl`. For real (non-mock) vectors you also need the optional peer dependency:

```sh
npm install @huggingface/transformers
```

## Build them

<Steps>

1. **Export the lexical index.**

   ```sh
   dockg export --format search
   ```

   ```
   Wrote 20 nodes to kg/localizations.json
   ```

   The indexes live beside the graph rather than inside it. Prose belongs in neither. An index
   holds the short, searchable strings a node already carries (titles, labels), not document text.

   **One index per language** ([localization](/dockg/model/localization/)): `search.<lang>.json`
   for each language the corpus declares, `search.und.json` for documents that declare none, and
   `localizations.json` naming them all.

   `und` tracks what the documents *say*, not how many languages there are. A corpus that labels
   everything `en` gets `search.en.json` and a one-entry manifest, while a corpus that labels
   nothing gets `search.und.json`. Both are monolingual as far as `--lang` is concerned.

2. **Compute vectors.**

   ```sh
   dockg embed
   ```

   ```
   Wrote 1 sidecar beside kg/localizations.json
     und: 20 vectors (384-d, onnx-community/granite-embedding-small-english-r2-ONNX) -> kg/vectors.und.bin
     20 embedded, 0 from cache
   ```

   `embed` reads the manifest as its work list and writes one sidecar per language, each with
   that language's model from `embed.byLanguage`. It then fills each entry's `vectors` block, so
   the manifest describes exactly what is on disk.

   For an offline or CI run with no model download, use deterministic hash vectors:

   ```sh
   dockg embed --model mock
   ```

3. **Query it.**

   ```sh
   dockg search "configuration" --limit 3 --mode lexical
   ```

   ```
   0.0164  https://example.com/kg/concept/configuration [skos:Concept] — Configuration
   0.0161  https://example.com/kg/doc/docs/configuration.md#configuration [dockg:Section] — Configuration
   0.0159  https://example.com/kg/doc/docs/configuration.md [dockg:Document] — Configuration Reference

   3 results (lexical)
   ```

   Drop `--mode lexical` and the same query runs `hybrid` when a sidecar is present: both legs
   score, and the fusion reorders. The trailing `(lexical)` / `(hybrid)` tells you which happened,
   so a run that quietly lost its vector leg is visible rather than merely worse.

</Steps>

## Embedding is local by contract

No API, no key, no spend, and **no corpus text leaves the machine**. That is a procurement fact as
much as a technical one, and it is worth having quotable.

Node and the browser **rank the same, though they do not compute the same floats**.
transformers.js uses a native ONNX runtime in Node and WASM in the browser. It accepts
*different* `device` values on each, so there is no way to force one backend everywhere. Measured
on the default model, a query embedded in Node and the same query in Chrome agree to
**cosine 0.999914**.

What dockg guarantees is **decisive ordering**, gated in CI against the real model in a real
browser. Any two results clearly separated in score keep their relative order on both platforms.
Results within the noise of each other may swap, and in practice sometimes do. Do not treat the
tail of a result list as stable across platforms. dockg pins what is pinnable: single-threaded on
the WASM side, `q8` weights, one text per call.

## The three modes

| `--mode` | Runs | When |
|---|---|---|
| `hybrid` | Lexical and vector, fused | Default when a sidecar and embedder are both available |
| `lexical` | Lexical only | Always available; no sidecar needed |
| `vector` | Vector only | Diagnosing the vector leg in isolation |

A missing embedder **degrades to lexical**, unless you asked for `vector` or `hybrid` explicitly,
in which case it errors. Silent degradation is fine when you did not express a preference and wrong
when you did.

## Staleness is refused, not ranked

A vector sidecar built from a different corpus, model, dtype, or dimension count would still
produce numbers. Those numbers would be meaningless, and the results would look normal.

dockg refuses instead:

```
dockg: Vector index was built from a different search index — the corpus changed. Re-run `dockg embed`.
```

```
dockg: Not a dockg vector index: bad magic — is this the right file? — re-run `dockg embed`, or use `--mode lexical`.
```

Exit `2` in both cases.

<Aside type="caution" title="Rebuild order matters">
  The sidecar is keyed to the search index, which is keyed to the graph. Change the corpus and all
  three must be rebuilt in order:

  ```sh
  dockg build && dockg export --format search && dockg embed
  ```

  Skipping the last step gives you a refusal at query time rather than bad results, which is the
  intended failure. It will still fail your pipeline, so wire the three together.
</Aside>

## Do not commit the sidecar

`kg/vectors.*.bin` is gitignored by default. They are binaries, they are derived, and
regenerating them requires a model download. They belong in your deploy pipeline rather than
your repository. The graph, the indexes and the manifest are text and can reasonably be
committed.

## Where to go next

<CardGrid>
  <LinkCard
    title="Query from the browser"
    href="/dockg/retrieve/runtime/"
    description="Load these artifacts and apply a scope filter, with the trace."
  />
  <LinkCard
    title="Embedding models"
    href="/dockg/reference/embed-models/"
    description="The tested set, their context limits, and the truncation trap."
  />
</CardGrid>

{/* Doc Detective inline tests: verify this page against the local build, exposed
    as `dockg` via `npm link` (see .github/workflows/doc-detective.yml), over the
    fixtures in test/fixtures/dd/. Writes go to .tmp/, never into a fixture.
    Two things to know before editing these. Output is asserted with `stdio`,
    which matches stdout or stderr; `stdout`/`stderr` are not schema properties,
    and a step using them is dropped without failing the run (scripts/check-doc-
    tests.mjs is the guard against that). And paths are repo-root-relative with
    the input globs in the fixture config, because a glob on the command line
    would close this comment. Run with `npx doc-detective`. */}
{/* test {"testId":"retrieve-artifacts","description":"The artifact chain: search index, vectors, and a ranked query."} */}
{/* step {"description":"Build the graph the artifacts derive from.","runShell":{"command":"dockg build -c test/fixtures/dd/dockg.config.yaml","exitCodes":[0]}} */}
{/* step {"description":"export --format search writes one index per language plus the manifest.","runShell":{"command":"dockg export -c test/fixtures/dd/dockg.config.yaml --format search -o .tmp/dd","exitCodes":[0],"stdio":"localizations.json"}} */}
{/* step {"description":"embed --model mock computes deterministic vectors with no download and no network.","runShell":{"command":"dockg embed -c test/fixtures/dd/dockg.config.yaml -i .tmp/dd --model mock","exitCodes":[0],"stdio":"mock)"}} */}
{/* step {"description":"search ranks graph nodes, fusing both legs when a sidecar exists.","runShell":{"command":"dockg search configuration -c test/fixtures/dd/dockg.config.yaml -i .tmp/dd --vectors .tmp/dd/vectors.und.bin --limit 3","exitCodes":[0],"stdio":"(hybrid)"}} */}
{/* step {"description":"Build the two-locale corpus for the refusal below.","runShell":{"command":"dockg build -c test/fixtures/dd/l10n.config.yaml","exitCodes":[0]}} */}
{/* step {"description":"Its indexes fan out into one file per language.","runShell":{"command":"dockg export -c test/fixtures/dd/l10n.config.yaml --format search -o .tmp/dd-l10n","exitCodes":[0],"stdio":"localizations.json"}} */}
{/* step {"description":"A single-language corpus needs no --lang; a multi-language one refuses to guess.","runShell":{"command":"dockg search installieren -c test/fixtures/dd/l10n.config.yaml -i .tmp/dd-l10n","exitCodes":[2],"stdio":"more than one localization"}} */}
{/* step {"description":"With --lang it answers, out of that language only.","runShell":{"command":"dockg search installieren -c test/fixtures/dd/l10n.config.yaml -i .tmp/dd-l10n --lang de","exitCodes":[0],"stdio":"l10n/de/install.md"}} */}
{/* step {"description":"A missing manifest is an operational error with the fix in the message.","runShell":{"command":"dockg search configuration -c test/fixtures/dd/dockg.config.yaml -i .tmp/dd/absent","exitCodes":[2],"stdio":"Localization manifest not found"}} */}
{/* test end */}
